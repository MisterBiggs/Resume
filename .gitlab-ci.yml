image: node

pages:
  stage: deploy
  script:
    - npm install -g resume-cli
    - resume export index.html --theme onepage
    - mkdir .public
    - cp -r ./site/* .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
