image: node

pages:
  stage: deploy
  script:
    - npm install resume-cli
    - npm install jsonresume-theme-onepage
    - resume export index.html --theme onepage
    - mkdir .public
    - cp -r ./site/* .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
